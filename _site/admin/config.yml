backend:
  name: git-gateway
  branch: master 

media_folder: "static/img" # Media files will be stored in the repo under images/uploads

collections:
  - name: "sections"
    label: "Section"
    folder: "sections"